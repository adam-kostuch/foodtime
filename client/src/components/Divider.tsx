import { Box, styled } from '@mui/material';

const Divider = styled(Box)({
  height: '95%',
  boxShadow: '0px 2px 4px -1px rgba(0,0,0,0.9), 0px 4px 5px 0px rgba(0,0,0,0.9), 0px 1px 10px 0px rgba(0,0,0,0.9)',
  backgroundColor: 'rgba(0,0,0,0.3)',
  width: '1px',
});

export default Divider;
